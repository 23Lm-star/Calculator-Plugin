# 独立审计报告 | M01-F03 | 2026-08-08

## 结论

**拒绝。** `release/Payload.zip` 存在于工作区但不在 Git 索引。完整克隆或下载已提交源码时不会取得安装器唯一依赖的载荷，故不满足“仓库源码直接安装交付”的首项验收标准。

## P1 修复交接

- 证据：`git ls-files -- release/Payload.zip` 无输出；`git status --short` 显示 `?? release/Payload.zip`。
- 影响：`scripts/release/Install-Calculator.cmd` 会调用 `Install-Calculator.ps1`，后者在 [Install-Calculator.ps1](../../../../../scripts/release/Install-Calculator.ps1:13) 固定定位仓库根目录的 `release\Payload.zip`，并在该文件缺失时于第 15-16 行终止。因此干净检出无法安装。
- 修复：将已审计的 `release/Payload.zip` 加入 Git 索引并纳入候选提交；确认 `.gitignore` 没有排除该精确文件。不要更改安装器路径、重建逻辑或应用源码。
- 复测：从仅含已提交文件的新克隆/归档副本运行 `scripts\release\Install-Calculator.cmd` 的受控 `-NoLaunch` 验证，确认 `release/Payload.zip` 存在且 SHA-256 为 `87BE6060E0BB4BADBC0658FB162175F1310C48F388E71BF4641BDD0895A3E101`，再重跑独立测试与独立审计。

## 已通过的复核

- CMD 只调用同目录 `Install-Calculator.ps1`；README 仅推荐完整获取项目后在 `scripts\release` 运行 CMD，PowerShell 仅作为等价备用入口，未要求外部 Installer ZIP。
- 三个 `scripts/release/*.ps1` 均为零 AST 解析错误。
- 现有工作区 Payload 为 56,029,144 字节、19 个条目；条目与固定白名单一致，未发现绝对路径、UNC 或 `..` 条目，包内文本也无此类泄漏。
- `Package-Release.ps1` 对构建内容使用显式白名单，并在归档前检查包内文本、归档后检查条目；防护规则覆盖盘符、UNC、扩展路径和 `..`。
- `Create-DesktopShortcut.ps1` 仅接受无目录分隔的快捷方式文件名。
- 独立测试 JSON 记录：受控副本不含 `build` 与 `artifacts/release`；进程内 `Start-Process` 替身未执行真实 `vc_redist.x64.exe`；载荷解压、应用复制、快捷方式调用、`/install /quiet /norestart`、退出码 `0`/`3010`/`1603` 分支和 `-NoLaunch` 均已覆盖。

真实 VC 运行库安装未执行，符合任务约束；本次拒绝仅由未受跟踪载荷导致。

## Git 记录

开始快照与结束快照见 `evidence/independent-audit/repository-payload-audit-2026-08-08.md`。除本报告和该审计证据外，本会话未修改任何文件；结束时分支为 `codex/release-delivery-v1.0`、HEAD 为 `0fd9edeeadf87a5738a170dc9ae1a0c1286387d2`。

## NEXT_SESSION_PROMPT

```text
$development-os 修复 仓库源码直接安装交付 docs/ai-workflow/tasks/m01-f03-release-delivery-v1/

只处理独立审计 P1：将已验证的 release/Payload.zip 纳入 Git 索引和候选提交，确认它未被 .gitignore 排除。不得修改应用源码、发布脚本、构建流程、依赖、分支或 Git 历史，不推送、部署或合并。完成后，从仅含已提交文件的受控新克隆/归档副本验证 CMD 可找到 release/Payload.zip；随后重新进入独立测试和独立审计。
```

## Re-entry Audit: P1 Candidate State | 2026-08-08

**REJECT.** The requested P1 staging action is complete: `release/Payload.zip` is staged and `git check-ignore --no-index -- release/Payload.zip` returned exit code `1`, confirming no ignore rule matches it. However, this does not make the file available to a clone that contains committed files only.

Independent controlled-clone evidence: clone exit `0`; `release/Payload.zip` absent; CMD exit `1`; failure path `scripts\\release\\Payload.zip`. The clean-clone flow therefore still cannot locate the root-level repository payload. The existing unstaged installer change that would use `release\\Payload.zip` is intentionally outside this repair's staged scope.

Required decision: authorize a coherent candidate commit that includes the already-verified repository-relative installer change and `release/Payload.zip`, or amend the scope to permit another valid delivery boundary. No commit was created here because the repair instruction prohibits Git-history changes.
