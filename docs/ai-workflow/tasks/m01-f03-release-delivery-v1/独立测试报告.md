# 独立测试报告 | M01-F03

## 结论

**通过（修复复测）。** P1/P2 修复后的发布交付物满足任务卡和审计复测项。未执行真实 `vc_redist.x64.exe`；三种 VC 安装结果均由本测试 PowerShell 进程内的 `Start-Process` 双替身返回，不会改变开发机运行库。

## 执行记录

- 分支与基线提交：`codex/release-delivery-v1.0`，`1ce0e9a46700fb80e0389457c2dc0452864d0003`。
- 主验证命令：`powershell.exe -NoProfile -ExecutionPolicy Bypass -File docs/ai-workflow/tasks/m01-f03-release-delivery-v1/evidence/independent-test/run-independent-validation.ps1`
- 主验证退出码：`0`。
- 临时输出仅位于任务证据目录，验证结束已清理；保留的断言日志和 JSON 结果见 `evidence/independent-test/`。

## 验收结果

| 验证项 | 结果 | 关键断言 |
| --- | --- | --- |
| PowerShell 解析 | 通过 | 三个 `scripts/release/*.ps1` 文件均为 0 个解析错误。 |
| 临时 ZIP 生成与运行时文件 | 通过 | 便携 ZIP 含 Qt5Core/Gui/Widgets、`platforms/qwindows.dll`、MSVCP140、VCRUNTIME140、VCRUNTIME140_1 与 `vc_redist.x64.exe`。 |
| ZIP 白名单与路径安全 | 通过 | entry 无绝对、UNC、扩展路径或 `..`；未白名单插件/翻译未入包；盘符、UNC、穿越文本样本均被拒绝。 |
| VC 退出码 `0` | 通过 | 精确参数为 `/install /quiet /norestart`，等待子进程并完成复制与快捷方式创建。 |
| VC 退出码 `3010` | 通过 | 安装成功，输出要求重启；`-NoLaunch` 未启动应用，且没有跳过 VC 调用。 |
| VC 退出码 `1603` | 通过 | 安装失败消息包含 `exit code 1603` 及部署目录内 `vc_redist.x64.exe` 的手动执行绝对路径。 |
| 安装与启动回归 | 通过 | 三个分支均复制 EXE 并创建桌面快捷方式；便携包实际启动 EXE 后进程存活，随后被测试清理。 |

## 临时交付物哈希

- `WangChenyangCalculator-1.1.0-independent.zip`：`AB95087DCE80D9C9D89237DC380BD000B3818F6FB3E69C46DBA77B0372C79A99`
- `WangChenyangCalculator-1.1.0-independent-Installer.zip`：`3CE94E6FC1CC0F97FC04C3D0F3F28412F50C724AA11CB30B48059AC1B4263A50`

## Git 范围核查

- 开始和结束的 `git status --short` 路径集合一致；已跟踪 diff 仍仅为 `.gitignore`、`docs/ai-workflow/任务总览.md`、`docs/ai-workflow/项目时间流.md`，共 14 行新增。
- 这些既有工作区改动及未跟踪的发布目录在测试开始前已存在，未由本会话修改。
- 本会话仅写入本任务的独立测试报告和 `evidence/independent-test/` 下的验证器、日志及结果；未修改源码、发布脚本、配置、现有测试、分支、提交或 Git 历史。

## 证据

- `evidence/independent-test/independent-validation.log`
- `evidence/independent-test/independent-validation-results.json`
- `evidence/independent-test/run-independent-validation.ps1`

## 风险与后续

真实 VC 运行库安装按任务约束未执行；上线或最终部署前仍应由发布方在目标环境决定是否运行该安装程序。本机桌面目录为只读，安装器分支测试以进程内 WScript Shell 双替身验证快捷方式创建调用；便携 EXE 启动仍为真实进程回归。

## NEXT_SESSION_PROMPT

```text
$development-os 独立审计 发布交付物生成与部署脚本化 docs/ai-workflow/tasks/m01-f03-release-delivery-v1/

你是独立、只读审计会话。读取任务卡、manifest、开发报告、独立测试报告和当前 diff；开始前后记录 Git status/diff。不得修改源码、脚本、配置、现有测试、分支或提交。审计 ZIP 打包白名单、路径泄漏防护、安装器错误处理与 VC 运行库调用边界；复核独立测试证据。若发现问题，生成精确修复交接，不得自行修复。
```
