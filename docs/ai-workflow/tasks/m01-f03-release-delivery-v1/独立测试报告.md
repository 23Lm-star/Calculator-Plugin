# Independent Test Report | M01-F03 | 2026-08-08

## Verdict

PASS. A complete checkout can use `scripts/release/Install-Calculator.cmd` as the only recommended entry point. The CMD entry invokes the repository-relative installer, and that installer locates `release/Payload.zip`; no external Installer ZIP is referenced by the end-user flow.

No production source, release script, configuration, existing test, branch, commit, or Git history was modified by this test session. The real `vc_redist.x64.exe` was never run.

## Starting Git Record

`git status --short`, `git diff --stat`, and `git diff --name-only` were captured before testing. Existing development changes were already present in task records, `release/README.md`, `scripts/release/Install-Calculator.ps1`, `scripts/release/Package-Release.ps1`, `release/Payload.zip`, and related untracked delivery files. They were preserved.

## Commands And Exit Codes

| Command | Exit code | Result |
| --- | ---: | --- |
| PowerShell AST parse for every `scripts/release/*.ps1` | 0 | `Create-DesktopShortcut.ps1`, `Install-Calculator.ps1`, and `Package-Release.ps1`: zero parser errors. |
| Payload hash and ZIP inventory | 0 | `release/Payload.zip`: 56,029,144 bytes; SHA-256 `87BE6060E0BB4BADBC0658FB162175F1310C48F388E71BF4641BDD0895A3E101`. |
| ZIP whitelist and path-leak scan | 0 | 19 file entries exactly match the package whitelist; no absolute, UNC, or `..` entries; no absolute, UNC, or `..` text leak in archive text files. |
| `C:\tmp\m01-f03-independent-install-validation.ps1` | 0 | Controlled copy with only `scripts/release` and `release/Payload.zip`; no `build` or `artifacts/release`. |

## Assertions

| Check | Result | Evidence |
| --- | --- | --- |
| Repository delivery entry | Pass | `Install-Calculator.cmd` invokes only `Install-Calculator.ps1`; installer computes the project-root `release\\Payload.zip` location. README names this as the only recommended installation flow. |
| Required payload files | Pass | EXE, Qt5Core/Gui/Widgets, `platforms/qwindows.dll`, MSVCP140, VCRUNTIME140, VCRUNTIME140_1, `vc_redist.x64.exe`, `Create-DesktopShortcut.ps1`, and README are present. |
| Controlled installer: locate, extract, copy | Pass | The isolated copy found `release/Payload.zip`, extracted it, and copied EXE and `vc_redist.x64.exe` to each test install directory. |
| Desktop shortcut invocation | Pass | A controlled-copy observer supplied inside the test payload was invoked by the installer after copy; no user desktop was modified. The production shortcut script separately passed AST parsing. |
| VC command and success branches | Pass | Process-local `Start-Process` substitute observed `vc_redist.x64.exe /install /quiet /norestart` with `-Wait -PassThru`. Exit codes `0` and `3010` completed successfully. |
| VC failure branch | Pass | Simulated `1603` failed with the exact exit code and the installed `vc_redist.x64.exe` path for manual execution. |
| `-NoLaunch` | Pass | VC invocation still occurred; no application launch was observed for all simulated result branches. |

## Gaps And Risks

There are no acceptance gaps from this independent test. Real VC runtime installation was deliberately excluded by task constraint. The shortcut workflow was verified through an observer embedded only in the controlled test payload, avoiding changes to the actual desktop; the original shortcut script was AST-parsed.

## Evidence

- `evidence/independent-test/repository-payload-independent-validation-2026-08-08.json`

## Next Session

```text
$development-os 独立审计 仓库源码直接安装交付 docs/ai-workflow/tasks/m01-f03-release-delivery-v1/

You are an independent read-only audit session. Read the task card, manifest, development report, this independent test report, evidence, project facts, and current diff. Record Git status/diff before and after. Do not modify source, release scripts, configuration, tests, branches, commits, or history. Audit archive whitelist/path defenses, end-user entrypoint, installer error handling, VC runtime boundary, and the adequacy of controlled-copy evidence. Write only the audit report and audit evidence. Record precise repair handoff only for findings.
```

## Re-entry: Candidate-Index Clone Check | 2026-08-08

**Result: FAIL.** `release/Payload.zip` was added as the only staged path and is not ignored. Its staged blob is `cdb3a3f9c51dd1b097e2f86a3f289fd13283ddd3`; SHA-256 is `87BE6060E0BB4BADBC0658FB162175F1310C48F388E71BF4641BDD0895A3E101`.

A controlled `git clone --no-local --no-hardlinks` from `HEAD` completed with exit code `0`. The clone did not contain `release/Payload.zip`, because index changes are not committed. Running `scripts\\release\\Install-Calculator.cmd` exited `1` before any VC runtime action and reported that `scripts\\release\\Payload.zip` was missing. The `HEAD` installer still uses that sidecar path, so this narrowly scoped staged change cannot satisfy the clean-clone acceptance check.

No application source, release script, build process, dependency, branch, commit, push, deployment, merge, or VC runtime installation occurred during this re-entry test.

## Re-run: Candidate Commit Clean-Clone Check | 2026-08-08

**PASS.** After the authorized local candidate commit `b591d6db69cac68e94f7989e113012fc631a15c1`, a fresh `git clone --no-local --no-hardlinks` contained `release/Payload.zip`. The file measured 56,029,144 bytes with SHA-256 `87BE6060E0BB4BADBC0658FB162175F1310C48F388E71BF4641BDD0895A3E101`.

`Install-Calculator.cmd` invokes its sibling `Install-Calculator.ps1`; the installer resolves the repository-root `release\Payload.zip`. In the clean clone, `validate-repository-payload.ps1 -ProjectRoot <clone>` exited `0` with 23 assertions passing: all three PowerShell scripts parsed, archive whitelist and path checks passed, required runtime files existed, and a process-local VC substitute verified an isolated `-NoLaunch` install without `build` or `artifacts/release`. The test did not execute real `vc_redist.x64.exe`.

Evidence: `evidence/independent-test/candidate-commit-clean-clone-validation-2026-08-08.md`.

## Integration Pre-Merge Re-run | 2026-08-08

**PASS.** A separate fresh clone of candidate `b591d6d` was validated again before any integration action. `release/Payload.zip` was present with SHA-256 `87BE6060E0BB4BADBC0658FB162175F1310C48F388E71BF4641BDD0895A3E101`; its 19 file entries passed whitelist and path safety checks. The 23-assertion controlled `-NoLaunch` validation passed, including AST parsing, payload resolution through CMD and its sibling PowerShell script, shortcut invocation, and intercepted VC arguments `/install /quiet /norestart`. No real VC runtime installer ran.

Evidence: `evidence/integration/candidate-premerge-rerun-2026-08-08.md`.

## Repair Rerun | 2026-08-08

PASS. In the detached isolated candidate worktree, the repaired package script and full validation harness completed with exit code `0`. `Package-Release.ps1 -OutputDirectory <dir>` produced `<dir>\Payload.zip`; the default tracked payload target was preserved. `engine_tests.exe` printed `All CalculatorEngine tests passed.`

The full harness recorded 48 passing assertions: three AST parses, payload existence, required runtime files, archive path safety, whitelist rejection, drive-letter/UNC/traversal text rejection, repository-relative controlled install, shortcut invocation, `-NoLaunch`, and VC `0`/`3010`/`1603` behavior. VC was intercepted by a process-local `Start-Process` replacement; no real `vc_redist.x64.exe` ran.
