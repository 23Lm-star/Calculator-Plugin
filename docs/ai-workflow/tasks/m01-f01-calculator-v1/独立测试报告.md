# 独立测试报告

- 任务：应届机试题-计算器业务需求包 v1.0 (`m01-f01-calculator-v1`)
- 工作流版本：`v1.0`
- 结论：通过，可进入独立审计。
- 测试时间：2026-08-03

## 已通过

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| Engine 全量单元测试 | `tests\\engine\\release\\engine_tests.exe` | 退出码 `0`；11 个正常、边界和错误场景通过 |
| Qt Release 构建 | `vcvars64.bat && nmake /nologo /f Makefile.Release`，工作目录 `build\\app` | 退出码 `0` |
| Qt 版本 | `qmake -query QT_VERSION` | `5.9.5`，退出码 `0` |

## 手工 UI 验收

用户于 2026-08-03 在本机手工完成并确认以下 UI 流程结果正确：窗口标题和初始显示、按钮四则运算、括号优先级、小数与一元负号、连续一元符号、`CE`、退格、结果续算、键盘输入、除零提示、非法字符提示、格式错误提示，以及异常输入不崩溃。

此前 Windows 应用自动化的应用控制授权等待超时，未取得窗口句柄；因此没有自动化截图、控制台错误或网络证据。该限制不影响用户已确认的手工测试结论，但审计阶段应将其视为证据留存不足的残余风险。

## Git 快照

测试前后状态一致，测试会话未修改受控源码、配置、既有测试、分支或提交。

开始前已存在以下非本会话变更，未处理：

- `calculator.pro`：未提交修改，将无条件 `/utf-8` 参数改为 `msvc:` 条件参数；该根目录路径也不在任务卡允许路径中。
- `calculator.pro.user`：未跟踪 Qt 用户配置文件。

## 独立审计交接

独立测试通过。审计时应核对 Engine 覆盖范围、MVC 分层和异常处理；同时记录 UI 验证仅有用户手工确认、没有截图附件这一残余证据风险。

```text
$development-os 独立审计 应届机试题-计算器业务需求包 v1.0 docs/ai-workflow/tasks/m01-f01-calculator-v1/
核对任务卡、开发报告、独立测试报告和实际 Git 差异。不得修改源码、配置、既有测试或 Git 历史。
```
