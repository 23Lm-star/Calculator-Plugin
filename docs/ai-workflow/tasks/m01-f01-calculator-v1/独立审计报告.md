# 独立审计报告

- 任务：应届机试题-计算器业务需求包 v1.0 (`m01-f01-calculator-v1`)
- 工作流版本：`v1.0`
- 审计日期：2026-08-03
- 结论：通过，可进入集成候选合并。

## 决定依据

审计开始前及结束后，Git 状态均包含以下与交付直接相关的项目配置修改：

- `.gitignore`：新增 `*.pro.user` 忽略规则。
- `calculator.pro`：将 `/utf-8` 从无条件参数改为 `msvc:` 条件参数。

`.gitignore` 防止 Qt 用户级 `*.pro.user` 文件进入版本控制；`calculator.pro` 将 MSVC 专属的 `/utf-8` 参数限制在 MSVC 构建条件下，保持 qmake 项目在其他工具链上的兼容性。二者均是本需求包的必要配置维护，不构成无关变更或流程阻断。

## 已核查项

| 项目 | 证据 | 结论 |
| --- | --- | --- |
| Model 与 Qt 解耦 | `src/model` 未包含 Qt 头或 Qt 类型；Engine 测试工程显式移除 Qt 模块 | 通过 |
| MVC 调用边界 | View 仅发出输入信号；Controller 唯一调用 `CalculatorEngine::evaluate` | 通过 |
| 表达式与异常处理 | 递归下降解析覆盖优先级、括号、小数和一元正负号；结构化错误覆盖空表达式、非法字符、非法小数、语法、括号和除零 | 通过 |
| Engine 回归 | `tests\\engine\\release\\engine_tests.exe`，退出码 `0`，输出 `All CalculatorEngine tests passed.` | 通过 |
| 差异质量 | `git diff --check e2ce7e6..HEAD`，退出码 `0` | 通过 |
| UI 验证证据 | 已有用户人工验收记录；无自动化截图或窗口控制证据 | 残余证据风险 |

## 集成交接

未发现阻断性业务代码、权限、数据泄露、第三方依赖或公共接口风险。残余风险是 UI 验证仅保留用户人工验收记录，缺少自动化截图或窗口控制轨迹；该风险不阻断本地集成，但应在集成回归时补强。

```text
新建集成会话：以当前功能分支候选合并至 integration，运行完整回归；仅在回归全部通过后执行本地集成。不得推送远程、合并 main 或部署。
```
