# 本地集成报告 | M01-F02

## 结论

本地集成前完整回归通过，允许将 `codex/calculator-desktop-v1.1` 合并到本地 `integration`。本节点不推送、不合并 `main`、不打标签、不部署，且未修改 `src/model/`。

## 回归结果

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| `build/v1.1-app` Release qmake/nmake | 通过，退出码 0 | `evidence/integration/logs/qt-release-build.log` |
| Release 目录 EXE、Qt 运行时和 3 个 MSVC CRT DLL | 通过 | `evidence/integration/release-directory-listing.txt` |
| `tests/engine` Release 构建与运行 | 通过，退出码 0，输出 `All CalculatorEngine tests passed.` | `evidence/integration/logs/engine-release-build-and-test.log` |
| 实际 Release EXE 直接启动 | 通过，窗口标题为 `wcy的计算器`，恢复既有自定义背景 | `evidence/integration/release-custom-background.png` |
| 固定窗口与图标 | 通过；Release 源码设置 `setFixedSize(600, 800)`，EXE/Qt 窗口均使用 `:/icons/calculator.ico` | `src/view/CalculatorUI.cpp`、`src/main.cpp`、`calculator.pro` |

所有执行命令、完整输出和退出码已写入 `evidence/integration/`。Git 基线和当前功能分支相对 `integration` 的差异见 `evidence/integration/git-before-integration.md`。

## 已知限制

- 预设主题关闭并重启后回退 Ocean 的问题，按用户明确决定暂不处理；不再为此修改源码。本次仅记录，不作为本地集成阻断项。
- 自定义背景渲染已由用户确认正常；桌面自动化的 Qt 窗口捕获保留背景截图。OS 截图辅助工具在该会话超时，未影响 EXE 启动或回归结论。

## 合并记录

已于 2026-08-05 合并到本地 `integration`：`37c94aa merge: integrate calculator desktop v1.1`。候选合并后再次执行 Release 构建和 Engine Release 测试，均以退出码 0 通过；后者输出 `All CalculatorEngine tests passed.`。

最终状态与合并关系见 `evidence/integration/git-after-integration.md`。本地工作区中仍保留未跟踪的 v1.0 发布材料和中文副本目录，未暂存、未纳入本次合并。

## NEXT_SESSION_PROMPT

```text
$development-os 发布准备 计算器桌面化增强与个性化配置 v1.1 docs/ai-workflow/tasks/m01-f02-calculator-desktop-v1-1/

仅读取本地 integration 的 M01-F02 集成记录、Release 目录和 evidence，完成发布准备检查。禁止推送、合并 main、打标签或部署。将预设主题重启后回退 Ocean 记录为已知限制，不修改源码；自定义背景渲染已确认正常。
```
