# 开发报告

## 结论

v1.1 实现收尾已记录，当前阶段为“待独立测试”。本次 diff 未触及 `src/model/` 或 `tests/engine/`，计算逻辑和既有 Engine 测试源码保持不变。

## 实现范围

- `resources/calculator.ico` 同时通过 `RC_ICONS` 和 `resources.qrc` 供 EXE、任务栏和窗口使用。
- `CalculatorUI` 使用 `setFixedSize(600, 800)`，提供 Ocean、Forest、Sunset 菜单和本地背景图选择。
- `ConfigManager` 以应用目录的 `calculator.ini` 保存 `profile/userName`、`appearance/themeId` 和 `appearance/backgroundImage`。
- 启动时 Controller 读取用户名；缺失时由 View 请求；保存后以“{用户名}的计算器”更新标题。主题和背景选择均经 Controller 持久化后再渲染。

## Git 与范围核对

- 分支：`codex/calculator-desktop-v1.1`；基线：`4856482`。
- 功能 diff 仅覆盖任务允许的工程、资源、配置、Controller、View 和项目文档路径。
- 工作区另有既存的中文副本工程和 `配置文件/` 图片等未跟踪项；它们不在本任务允许路径或实现范围内，未被本节点修改。
- `git diff --check` 在记录时无空白错误；Git 对全局 ignore 文件的“Permission denied”警告不影响该检查。

## 证据与结论边界

详见 [development evidence](evidence/development/development-evidence.md)。构建产物、Engine 可执行文件、Qt 部署目录和配置往返状态均已保留并记录；v1.1 没有保存可复核的构建命令日志、Engine 运行输出或 UI 截图。因此这些是开发阶段的产物证据，不替代独立测试的重新执行和验收。

## 残余风险

- 自定义背景保存绝对本地路径；图片被移动或删除后会回退到样式表的默认可用状态，但不会自动清理失效配置。
- 未保存 v1.1 的窗口自动化轨迹或截图；独立测试须在隔离运行目录完成首启、重启、主题、背景、图标和窗口尺寸验收。
- 未执行合并、打标、推送或部署。

## 下一会话

见 manifest 中的 `next_session_prompt`。未经用户明确指令，不进入独立测试。
