# 计算器

这是一个基于 Qt Widgets 的四则运算计算器。支持 `+`、`-`、`*`、`/`、小数、括号、一元正负号、`CE` 清空和退格。

## 环境要求

- Qt 5.9.5 或兼容的 Qt 5 Widgets 开发环境
- C++11 编译器
- qmake
- Windows 上使用 MSVC 时需要 Visual Studio C++ 工具链和 nmake

## 构建与运行

### Qt Creator

1. 打开 `calculator.pro` 并选择与 Qt 版本匹配的 Kit。
2. 以 Release 模式构建并运行 `WangChenyangCalculator`。

### Windows MSVC 命令行

在 Visual Studio 的 x64 开发者命令提示符中执行：

```powershell
qmake calculator.pro
nmake /f Makefile.Release
.\release\WangChenyangCalculator.exe
```

生成的可执行文件依赖 Qt 运行库。若需发布给未安装 Qt 的 Windows 用户，请在生成目录执行 `windeployqt WangChenyangCalculator.exe`，并分发生成的整个目录。

## 运行测试

在 `tests/engine` 目录构建并运行 Engine 回归测试：

```powershell
Set-Location tests\engine
qmake engine_tests.pro
nmake /f Makefile.Release
.\release\engine_tests.exe
```

成功时会输出 `All CalculatorEngine tests passed.`
